var IRC = require( './lib/irc' )
  , bot = new IRC({ server: 'irc.freenode.net' })

bot
  .addListener( 'privmsg', function( m ){ console.log( m ) } )
  .connect( function(){ this.join( '#runlevel6' ) } )
